# 精算利率监测平台 · 使用说明书

页面版本 v40.1（2026-09-16）
在线地址：https://roll820724-crypto.github.io/bond-yield-curve/
本地目录：~/hermes/工作/利率曲线/
（页面工具栏「📖 使用说明」按钮内嵌同一份内容，以页面内版本为最新；本文件便于离线查看与打印。）

---

## 一、这个页面是干什么的

用中国债券信息网（中债网）官方收益率曲线数据，监测与精算定价、评估相关的利率指标。三种模式：

| 模式 | 看什么 |
| --- | --- |
| ① 750相关分析 | 评估口径：以 750 日移动平均（750MA）为基准，看当日曲线与评估用曲线的差距、750 口径的期限利差、10Y 历史中枢与置信带、评估时点对比、曲线外推，以及历史 750MA 曲线底稿导出。 |
| ② 当日线相关分析 | 市场口径：国债/国开债即期与到期曲线对比、税收溢价（国开-国债 9Y 即期）、期限结构对比、10Y 走势、AAA 企业债较国开债溢价。 |
| ③ 预定利率动态追踪 | 按监管分段调节机制：利率走廊、基础回报水平、参考利率 M、预定利率 N、分段调节系数表及调整前后对比，附 LPR/定存验证数据。 |

## 二、数据来源、更新机制

- **来源**：中债网官方接口（yield.chinabond.com.cn），抓取 1Y–50Y 全期限结构；国债、国开债各取「即期」与「到期」两个口径，共 4 条核心曲线。
- **自动更新**（家里 Mac mini 上的定时任务）：每日 17:30 抓取当日数据 → 18:00 预计算模式三 → 18:10 提交并推送到 GitHub Pages。周末、节假日无新数据时不产生新版本。
- **数据版本**：version.json 记录版本号（形如 20260915181032，即 2026-09-15 18:10:32）与数据日期。页面所有资源都带上该版本号加载，用于穿透浏览器缓存——数据更新后正常刷新即可看到最新数据，不必手动清缓存。
- **怎么看数据新旧**：页面顶部「数据更新至 XXXX-XX-XX」就是当前展示的数据日期。

## 三、750MA 的计算口径（最重要，务必记住）

- **窗口**：对第 i 个交易日，取 `[i-749, i]` 共 750 个交易日（不是自然日、不是取 750 天前的单点），对该期限内该期限点的所有非空值取算术平均。
- **样本不足**：窗口内非空值不足 750 个时，按实际非空样本平均；窗口内交易日数少于 20 个时不出值（留空）。
- **暖机期**：数据自 2008-01-02 起，前约 750 个交易日（至 2011 年 1 月）样本不满，那一段的 750MA 不建议使用。
- **校验锚点**：2025-12-31 国债即期 1–20Y 各整数年期限 750MA 的均值 = 2.2315%（与手工底稿 2.232% 一致，差 0.05bp）。这个数可以用来验证任何一次口径调整是否做对了。

## 四、模式一各卡片

| 卡片 | 内容 |
| --- | --- |
| 📉 收益率曲线（即期 vs 750MA） | 基准日的当日曲线与当日 750MA 对比，并给出汇总数字。 |
| 📊 期限利差走势（750） | 750 口径的三条利差历史走势：10Y-1Y（期限溢价）、30Y-10Y（超长端定价）、5Y-1Y（中短端陡峭程度）；可切换过去 1 年 / 3 年 / 全部。 |
| 📋 关键期限 vs 750MA 偏离 | 1Y/3Y/5Y/7Y/10Y/15Y/20Y/30Y/50Y 的当日值、750MA 值及偏离（bp）。 |
| 📅 10Y 实际 vs 750MA ±1.96σ | 10Y 的 750MA 中枢与 750 日窗口标准差构成的置信带（自 2013 年起，更早样本不足），三色分区看偏离位置。 |
| 🔄 不同评估时点 750 对比 | 选两个时点，比较各自 750MA 的期限结构；「变动」= A − B（时点 A = 当前选择时点，时点 B = 参考时点）。 |
| 📈 曲线外推：750MA 预测 | 对未来 12 个季末的 750MA 做外推，三种口径：**基于日期**（未来每日曲线恒等于所选日曲线，向前滚动算出 750MA）；**基于中枢**（输入目标 10Y 中枢值，取历史上 10Y 最接近该值的日期）；**基于利差**（以基准日曲线形状为底，整体平移到目标 10Y 水平）。 |
| 📤 导出历史750曲线 | 历史 750MA 曲线底稿导出，见第六节。 |

## 五、导出功能一览

| 入口 | 导出内容 |
| --- | --- |
| 工具栏 📥 下载全量1-20Y | 工具栏当前曲线的**原始数据**（全部历史、1–20Y，非 750MA）。 |
| 各卡片右上角 📥底稿 | 该卡片当前展示内容的数值底稿（单时点、或图上区间的数值）。 |
| 卡片7 📥 一键导出 | **历史 750MA 曲线**底稿：可按关键日期，也可按区间；可同时导出多条曲线，见下节。 |

## 六、导出历史750曲线（卡片7，2026-09-16 新增）

### 6.1 两种导出方式

- **按关键日期**：输入框里填多个日期，换行、逗号、分号、空格分隔都认；日期写法 `2025-12-31`、`2025/12/31`、`2025.12.31`、`20251231`、`2025年12月31日` 均可。输出每个日期对应的 750MA 期限结构。**非交易日会自动取该日前最近一个交易日**，底稿中同时保留「请求日期」和「实际交易日」两列，不会张冠李戴。识别不了的词条会跳过并在卡片下方提示「已跳过」。
- **按区间**：选起始与结束日期，输出区间内**每个交易日**的 750MA 期限结构（非交易日没有 750 数据，因此只输出真实交易日）。区间内交易日数超过 1500 时会先弹确认框，告知数据量。

### 6.2 曲线勾选

可多选（国债即期 / 国开债即期 / 国债到期 / 国开债到期），**默认跟随工具栏所选曲线**；手动改动后就不再跟随。勾选多条时，每条曲线一个工作表（工作表名即曲线名），另加一张「计算说明」表。

### 6.3 底稿列结构

关键日期方式：`请求日期 | 实际交易日 | 1Y | 2Y | … | 50Y | 1-20Y均值 | 10Y-1Y(bp) | 5Y-1Y(bp) | 30Y-10Y(bp)`；区间方式去掉「请求日期」列。

衍生列口径：1-20Y均值 = 该行 1Y~20Y 各整数年期限 750MA 的算术平均（保留 6 位小数）；三个利差 = 由该行收益率列相减、四舍五入保留 2 位小数，单位 bp。衍生列可以直接用同一行的收益率列复算，结果一致。

### 6.4 文件名与规模

- 单曲线：`750MA曲线_关键日期_国债即期.xlsx` 或 `750MA曲线_2026-09-01至2026-09-15_国债即期.xlsx`；多曲线时曲线名替换为「N条曲线」。
- 实测：国债即期全区间（2008-01-02 至 2026-09-15，4766 个交易日 × 50 个期限）导出 7.29MB、耗时约 0.3 秒；4 条曲线同时导出约 30MB 量级，桌面端无压力，手机端较慢。

### 6.5 口径一致性

导出值与页面图表同源同算法：程序内部用滑动窗口累加计算（加当日、减第 750 日前的一天），与页面 calc750MA 在最新交易日 50 个期限上逐项比对差异为 0；与独立 Python 复算逐格比对差异在 5e-7 以内（6 位小数舍入）。

## 七、常见问题

- **手机或公司网络打开后文字、数字正常，但图表全白**：页面图表依赖外部 CDN（ECharts、SheetJS）。网络取不到 CDN 时会出现这种情况。换网络重试；若长期需要在受限网络用，可改为本地内嵌版本（需改 index.html 两个 script 引用）。
- **打开后数据没更新**：先看顶部数据日期；若仍是旧的，在地址后加 `?v=1` 强制刷新。若当天是节假日，本就没有新数据。
- **模式三的日期比模式一少一天**：模式三预计算排在 18:00，主数据 18:10 入库，因此模式三通常滞后一个交易日，页面会自动走前端兜底计算。
- **模式二里 4 条扩展曲线（企业债AAA、农发债、进出口行债、保险资本补充债）数据停在 2026-07-22**：抓数脚本目前只覆盖 4 条核心曲线，扩展曲线未纳入每日更新，属于已知未完成项。

## 八、维护备忘（写给以后的自己）

| 文件 / 事项 | 作用 |
| --- | --- |
| index.html | 整个页面：样式、三种模式、全部图表与导出逻辑都在里面（纯前端，ECharts + SheetJS）。 |
| update.py | 抓取中债网数据，写入 4 条核心曲线的 json 与 summary.json、version.json。文件头注明「引用自 yyseles/bond-yield-curve」。 |
| compute_m3.py | 模式三预计算，读国债/国开到期、LPR5Y、5年定存，输出 data_m3_computed.json（前端秒开用）。 |
| data.json / data_gov_ytm.json / data_cdb.json / data_cdb_ytm.json | 国债即期 / 国债到期 / 国开债即期 / 国开债到期，结构为 {dates:[], terms:[], rows:[[]]}。 |
| data_corp_aaa / data_adbc / data_exim / data_insurer.json | 模式二的 4 条扩展曲线（当前停更，见第七节）。 |
| data_lpr5y.json / data_deposit5y.json / data_m3_computed.json | 模式三输入与预计算结果。 |
| 手工更新一次 | `cd ~/hermes/工作/利率曲线 && python3 update.py`，然后按需 `python3 compute_m3.py`，再 `git add -A && git commit && git push`。 |
| 定时任务 | 17:30 抓数、18:00 模式三预计算、18:10 提交推送（家里 Mac mini 上的 cron）。 |
| 仓库与线上地址 | GitHub 仓库 roll820724-crypto/bond-yield-curve，Pages 地址见文首；推送后 1–5 分钟生效。 |

## 九、变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-16 | 新增卡片7「导出历史750曲线」：按关键日期 / 按区间两种导出方式，支持多曲线勾选，底稿增加 1-20Y均值与三个关键期限利差列；新增使用说明面板与本文件。 |
| 2026-07-16 | 三模式框架成型（750 / 当日线 / 预定利率），750MA 窗口统一为 750 个交易日，新增置信带、评估时点对比、曲线外推。 |
| 2026-07-14 | 仪表盘首次上线：四条核心曲线、750MA、期限利差、关键期限表，cron 每日 17:30 自动更新。 |
